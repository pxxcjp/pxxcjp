name: Patch fast scroller hitbox
on:
  push:
    paths: ['.fast-hitbox-trigger']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Patch CSS
        run: |
          python - <<'PY'
          p='index.html'
          s=open(p,encoding='utf-8').read()
          s=s.replace('.fast-track{position:absolute;z-index:42;right:2px;top:88px;bottom:78px;width:14px;touch-action:none}', '.fast-track{position:absolute;z-index:42;right:2px;top:88px;bottom:78px;width:28px;touch-action:none}')
          s=s.replace('.fast-label{position:absolute;z-index:43;right:20px;', '.fast-label{position:absolute;z-index:43;right:34px;')
          open(p,'w',encoding='utf-8').write(s)
          PY
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add index.html
          git commit -m 'Expand fast scroller hitbox and shift date label' || exit 0
          git push
